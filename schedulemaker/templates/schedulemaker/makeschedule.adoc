= Time to make the Schedule...

+++{% csrf_token %}+++

link:../loadpreset[Load your preset here]

You're logged in as {{ user.username }}. link:./logout[Log out]

{% if uuid %}
For debug purposes: your uuid is `+{{ uuid }}+`
{% endif %}

{% if form %}
++++
<form action="" method="post">
    {% csrf_token %}
    {{ form }}
    <input type="submit" value="Submit">
</form>
++++
{% endif %}

link:../[Go back home]